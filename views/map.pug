extends layout

block content
    .inner
        h2= title
        .map
            .autocomplete
                input.autocomplete_input(type="text" placeholder="Search for Anything" name="geolacate")
            #map
                p Loading Map ...
